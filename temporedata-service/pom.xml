<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.temporedata</groupId>
        <artifactId>temporedata</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>temporedata-service</artifactId>
    <name>temporedata-service</name>
    <description>业务服务域：sys/dev/ops/gov/svc/asset/integration</description>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.temporedata</groupId>
                <artifactId>temporedata-bom</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-security</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- P0-2 partial dedup: connectivity/datasource/secret/security + file low-level moved here -->
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-integration</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- P0: dataset/metadata capability (entities/repos/services) composed into the app -->
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-metadata</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- P0: quality (rule/gate) and asset (catalog/metric) capability -->
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-quality</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-asset</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- P1: data contract validation + Data API gateway -->
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-contract</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-service-gateway</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 数据源 SPI 已并入 temporedata-api；内置方言由本模块携带 -->
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-datasource-mysql</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-datasource-postgresql</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-datasource-clickhouse</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-datasource-oracle</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-datasource-doris</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-datasource-starrocks</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-datasource-hive</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.temporedata</groupId>
            <artifactId>temporedata-datasource-oceanbase</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- 数据源驱动：Maven 坐标动态解析依赖(Aether / maven-resolver) -->
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-impl</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-connector-basic</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-transport-http</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.resolver</groupId>
            <artifactId>maven-resolver-supplier</artifactId>
        </dependency>

        <!-- Spring Boot starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-quartz</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-websocket</artifactId>
        </dependency>

        <!-- Object storage (S3-compatible adapter, S4-A) -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
        </dependency>
        <!-- zstd compression (optional file-compress algorithm) -->
        <dependency>
            <groupId>com.github.luben</groupId>
            <artifactId>zstd-jni</artifactId>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>

        <!-- API documentation -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-ui</artifactId>
        </dependency>

        <!-- Workflow orchestration -->
        <dependency>
            <groupId>org.jgrapht</groupId>
            <artifactId>jgrapht-core</artifactId>
        </dependency>
        <!-- SQL parser (Druid, single parser for all dialects) -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>druid</artifactId>
        </dependency>

        <!-- Big data engines (provided, plug-in based execution) -->
        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-core_2.12</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-sql_2.12</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-clients</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-table-api-java</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-client</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.calcite</groupId>
            <artifactId>calcite-core</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Utilities -->
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
        </dependency>
        <dependency>
            <groupId>com.cronutils</groupId>
            <artifactId>cron-utils</artifactId>
        </dependency>
        <dependency>
            <groupId>com.jcraft</groupId>
            <artifactId>jsch</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct-processor</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>